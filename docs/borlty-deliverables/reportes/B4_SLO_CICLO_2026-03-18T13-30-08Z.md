# B4 SLO de entrega — Verificación de ciclo (2026-03-18T13-30-08Z)

## Criterio formal de cierre B4

B4 puede pasar a **CERRADO** cuando, en un ciclo operativo válido, se cumpla:

1. **Tasa de entrega >= 90%** en la ventana objetivo (24h por defecto).
2. **Fallidos <= 10%** sobre intentos efectivos (`enviados + fallidos`).
3. **Reprogramados controlados <= 20%** del total procesado.
4. Sin errores de contrato/API en endpoints B4:
   - `GET/PUT /api/notificaciones/preferencias`
   - `POST /api/notificaciones/scheduler/encolar`
   - `POST /api/notificaciones/procesar-cola`
   - `GET /api/notificaciones/metricas-entrega`

## Evidencia de ciclo (técnica local)

- Validación backend B4 ejecutada:
  - `backend/.venv/bin/pytest -q backend/tests/api/test_notificaciones_endpoints.py`
  - Resultado: **5 passed**
- Validación frontend de operación:
  - `npm run lint` ✅
  - `npm run build` ✅
- Componentes operativos activos:
  - métricas de entrega API (`/api/notificaciones/metricas-entrega`)
  - semáforo en UI de configuración (verde/amarillo/rojo)
  - límites por tipo de reintento (`NOTIF_MAX_INTENTOS_*`)

## Estado del ciclo

- **Resultado de este ciclo:** APTO TÉCNICAMENTE
- **Pendiente para cierre formal:** consolidar un ciclo con datos operativos reales (producción/staging activo con tráfico de notificaciones).

## Próximo paso recomendado

Ejecutar 1 ciclo operativo real (24h) y anexar snapshot de `/api/notificaciones/metricas-entrega` para cerrar B4 formalmente.
